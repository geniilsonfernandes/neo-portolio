import { DragToExpanded } from "@/modules/lab/components/drag-to-expanded";

export default async function LagPage() {
  return (
    <div className="py-4 min-h-screen flex justify-center items-end">
      <DragToExpanded />
      
    </div>
  );
}
